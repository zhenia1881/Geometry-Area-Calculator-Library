from setuptools import setup, find_packages

setup(
    name="geometry_calculator",
    version="0.1",
    packages=find_packages(),
    description="Geometry area calculator library",
    author="Zhenia",
    python_requires=">=3.7",
)